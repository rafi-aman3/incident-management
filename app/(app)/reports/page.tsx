import { EmptyState } from "@/components/empty-state";

export default function ReportsPage() {
  return (
    <EmptyState
      title="Reports landing coming in Phase 3"
      body="OSHA 300 / 300A live preview, OSHA 301 PDF, RIDDOR F2508 PDF — all powered by the live database."
    />
  );
}
