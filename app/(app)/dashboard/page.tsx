import { EmptyState } from "@/components/empty-state";

export default function DashboardPage() {
  return (
    <EmptyState
      title="Dashboard coming in Phase 1"
      body="KPIs (TRIR / DART), regulatory banners, and the recent-activity feed land here in Phase 1. For now you can navigate the rest of the shell."
    />
  );
}
