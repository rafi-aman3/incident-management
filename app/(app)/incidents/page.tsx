import { EmptyState } from "@/components/empty-state";

export default function IncidentsListPage() {
  return (
    <EmptyState
      title="Incidents list coming in Phase 1"
      body="Filterable, role-scoped table with severity badges, status pills and pagination."
    />
  );
}
