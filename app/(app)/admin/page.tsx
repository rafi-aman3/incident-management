import { EmptyState } from "@/components/empty-state";

export default function AdminPage() {
  return (
    <EmptyState
      title="Admin coming in Phase 3"
      body="Site Setup Wizard, role editor, notification recipients, and member invites ship here."
    />
  );
}
