import { EmptyState } from "@/components/empty-state";

export default function CapaPage() {
  return (
    <EmptyState
      title="CAPA module coming in Phase 2"
      body="Tabbed view: Active / Pending verification / Closed. Owner-cannot-verify is enforced at all 3 layers."
    />
  );
}
