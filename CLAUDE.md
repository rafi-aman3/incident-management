@AGENTS.md

# EHS Operations Platform — Project Context

## What this is
A production v1 EHS Operations Platform for SDS Manager. An organisation contains hierarchical sites; each site runs **Incidents** (capture → classify → route → investigate → CAPA → regulatory reports — OSHA in the US, RIDDOR in the UK), **Inspections** (against versioned templates, scheduled or one-off), and **Resources** (operational assets + linked documents). Everything surfaces in a unified **Planner** calendar.

Stakeholder demos are milestones inside the build, not a separate deliverable. `PLANNING/IMS_PLANNING.md` is the reference roadmap (a separate workstream pushes revisions directly to main); `docs/SPEC.md` §15 records where we intentionally diverge.

## Modules

| Module | Scope | What it does |
|---|---|---|
| **Incidents** | per-site | Report → classify (5×5 risk matrix) → route (Track A/B/C) → investigate (5-Why + evidence) → CAPA (with independent verification) → regulatory reports (OSHA 300/300A/301, RIDDOR F2508) |
| **Templates** | org-scoped, industry-typed | System presets per industry → cloned to org-level → optional per-site assignment. Versioned; in-flight inspections snapshot the template version. |
| **Inspections** | per-site | Run a template; scheduled (daily/weekly/monthly/custom) or one-off; failed items become findings; manual escalation to incident |
| **Resources / Assets** | per-site (org roll-up view) | Operational asset registry: location, condition, last-inspected, PM dates, SDS link |
| **Resources / Documents** | org-scoped | File uploads (PDF/DOC) polymorphically linkable to incident / inspection / CAPA / asset / site |
| **Planner** | unified | Read-only calendar of past + upcoming events across all modules |

## Tenancy

- An **organisation** owns everything. A user belongs to exactly one org.
- An org has multiple **sites**. Sites form a tree via `parent_site_id` (self-FK, nullable).
- Hierarchy is **user-centric** for inheritance: data (templates, assets, incidents, …) attaches to the specific site it was created on; access propagates down the tree only when `site_members.include_children = true`. See memory: `project_site_hierarchy.md`.

## RBAC

- **Roles** are org-defined rows (NOT an enum). Org owners can edit role permission sets. 4 default seeded roles per new org: `worker`, `supervisor`, `ehs_manager`, `site_admin`.
- **Teams** are **site-scoped**: a team binds to one or more sites (`team_sites`) and grants its permissions only within them.
- **Permissions** are system-keyed strings (`template:create`, `incident:close`, `capa:verify`, `member:invite`, `role:edit`, …). Not user-editable.
- Resolver in `lib/rbac/resolve.ts` unions role permissions (per `site_members`) with team permissions (per `team_members ∩ team_sites`). UI / Server Actions / RLS all key off this — never hardcode role checks.
- See memory: `project_rbac_model.md`.

## Read these in order before writing code

1. `AGENTS.md` — **Next.js 16.2.4 has breaking changes** from training data; consult `node_modules/next/dist/docs/` first
2. `docs/SPEC.md` — single source of truth: tenancy, RBAC, data model, workflow rules, regulatory triggers, RLS, enums. **§15 decisions log records every choice that's been locked in.**
3. `docs/ui-flow.md` — page-by-page contract: every route, permission gates, modals, state machines, URL conventions, build priority
4. `docs/onboarding.md` — slim onboarding (signup → org → site → invite → pathway → guided dashboard); welcome cards; tooltip inventory
5. `docs/design.md` — visual tokens (OKLch palette, brand purple), typography, component recipes (5×5 risk matrix, body map, 5-Why chain, regulatory banner, template builder, inspection runner, calendar grid), dark mode
6. `plans/00-foundation.md` (shipped) and `plans/01-incidents-capture.md` (shipped) — execution detail for Phases 0 and 1; per-phase plans for 02–06 written at the start of each phase
7. `PLANNING/IMS_PLANNING.md` — reference production roadmap; honor as default direction unless we've logged a divergence in SPEC §15

## Tech stack (locked)

- **Framework:** Next.js **16.2.4** + React 19.2.4 + Tailwind v4 + shadcn/ui (radix-vega style)
- **Database / Auth / Storage:** **Supabase**. Env var name uses the new convention: `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (NOT the older `ANON_KEY`)
- **Cache Components ON** (`cacheComponents: true` in `next.config.ts`); params are `Promise<...>` (must `await`); middleware is **`proxy.ts`** at the project root (NOT `middleware.ts`); `'use cache'` directive replaces old `fetch` cache options. **`export const runtime` is rejected by route handlers under Cache Components** — handlers default to nodejs anyway, so just omit the export (use `.tsx` extension when the handler returns JSX, e.g. for `@react-pdf/renderer`).
- **Forms:** `react-hook-form` + Zod + Server Actions + `useActionState`. Actions return `{ ok: true, data } | { ok: false, error, fieldErrors? }`
- **Other:** recharts (charts), @dnd-kit (Kanban drag), @react-pdf/renderer (OSHA 301 / RIDDOR PDF), date-fns

## Hard rules (don't break)

- **CAPA owner ≠ verifier** — enforced at 3 layers: UI (button disabled), Server Action (rejects), DB CHECK constraint (`verifier_id IS NULL OR verifier_id <> owner_id`). All three. The owner-cannot-verify property is independent of the role/team model — it's a per-CAPA invariant.
- **RBAC is roles + teams + permissions, not a role enum.** Use the resolver in `lib/rbac/resolve.ts`. Never write `if (role === 'site_admin')` — write `if (await can(user, 'permission_key', siteId))`. The 4 default role names (`worker` / `supervisor` / `ehs_manager` / `site_admin`) are seeded rows, not enum values, and admins can edit their permission sets.
- **Site hierarchy is access-only, not data.** Data attaches to one site; access propagates only via `site_members.include_children = true`. No template / asset / incident inheritance up or down the tree.
- **Brand purple `#735CDD`** with a **separate success green `#16A34A`** — NOT the same color. Severity S4 (Minor) and Track C ("Log & close") use the success green, not the brand. Don't propose another color swap; it's locked.
- **Notifications fire at classification, NOT at workflow end** — the regulatory clock (OSHA 8hr fatality, OSHA 24hr amputation, RIDDOR immediate, etc.) cannot wait for investigation completion. Engine in `lib/workflow/notifications.ts`.
- **No tour-overlay libraries** (joyride / shepherd / intro.js / driver.js / etc.) — UI must be self-explanatory via labels, tooltips, useful empty states. Setup goes through real multi-step wizards (Onboarding, Report Wizard, Template Builder) with dedicated routes. The dashboard is "guided" via empty-state cards and action tiles, not floating overlays.
- **Soft-delete only** on `incidents`, `investigations`, `capas` — never hard-delete (5y OSHA / 3y RIDDOR retention). Append-only audit tables (`severity_overrides`, `notifications`, `activity_events`) have UPDATE/DELETE revoked from PUBLIC.
- **3-step Report Wizard uses draft-row + per-step server actions**, not client-only state. The `incidents` row exists in DB from Step 1 with `status='draft'`; the regulatory clock starts on Step 3 finalize.
- **Workflow engine called from a Server Action, NOT a DB trigger** (severity engine, routing engine, notification engine all live in `lib/workflow/*.ts` and run inside `finalizeIncident`).
- **Verification result has 4 outcomes** (`effective` / `partially_effective` / `not_effective` / `too_early_to_verify`) plus a `verification_method` enum (`inspection` / `monitoring` / `audit_trend` / `re_interview` / `document_review`). `partially_effective` auto-creates a follow-up CAPA via `follow_up_capa_id`.
- **Templates are versioned; in-flight inspections snapshot the version.** Editing a template publishes a new version; running inspections keep their `template_version_id` snapshot so mid-cycle edits never silently rewrite history.
- **Argus is assistive, not authoritative.** The AI co-pilot suggests; humans decide. Severity, track, CAPA closure, OSHA/RIDDOR submission must always carry a named human signature. No tool-use call may finalize an incident, write a regulatory report, or close a CAPA. Inverts the IMS_PLANNING §17.8 / §2.4.3 "AI is a permanent non-goal" stance — see `docs/SPEC.md` §15 (2026-05-10) and §16. Never auto-classifies severity, auto-routes incidents, or auto-closes CAPAs.
- **Every Argus interaction is logged.** `argus_suggestions` row + `activity_events` row with `actor_kind='argus'`. Includes model, prompt + completion + cache tokens, and the user's outcome. Required for audit + cost accounting. Argus actions still gate through existing RBAC (`capa:create`, etc.) — there is no separate `argus:*` permission set; `argus:use` is the single feature-availability flag.
- **PII redaction before sending to the model provider.** `lib/argus/redact.ts` strips known names + emails + phone + UK NI before egress. Provider-agnostic: applies regardless of which adapter (`lib/argus/llm/`) is configured. Free-text descriptions can still re-leak; treat the redactor as defence-in-depth, not a guarantee. Cost guardrails are server-side: per-org daily token budget + per-user rate limit; both enforced in the route handlers before model calls.

## Conflict resolution

- Our docs (SPEC, design, ui-flow, onboarding, plans) are the source of truth for what we ship. When `PLANNING/IMS_PLANNING.md` and our docs disagree, resolve explicitly: either update our docs to match, or log the divergence in `docs/SPEC.md` §15. Never silently ignore an IMS_PLANNING update — it's owned by a separate workstream that pushes revisions to main, and we delta-check on each push.
- When two of our docs disagree: design.md wins for visual concerns; SPEC.md wins for data/workflow; ui-flow.md wins for page structure.

## Conventions

- Branch naming + commit conventions: `.claude/rules/github-workflow.md`. Commits use Conventional Commits prefix (`feat:`, `fix:`, `docs:`, `chore:`).
- All commits include the `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` trailer per the bash tool guidance.
- **Build status:** see [`docs/BUILD_STATUS.md`](docs/BUILD_STATUS.md) for the full phase-by-phase shipping log. Current state in one line: **V1 polish-complete + Phase 8 + Phase 12 + Phase 13 + Phase 9a + Phase 9b + Phase 9c + Phase 9d + Phase 9e + Phase 7 + Phase 10 shipped on `main`; Phase 9 closed; v1 phase list complete** — all 5 modules shipped 2026-05-06 + Phase 6 frontend polish closed 2026-05-08 (12 PRs) + Phase 11 org admin console (11a/11b/11c) + Phase 8 Settings (PR #24) + Phase 12 Auth & Onboarding (PR #25) + Phase 13 Site Setup OSHA + RIDDOR (PR #26) + Phase 9a Argus Foundation (PR #27) + Phase 9b Argus Copilot (PR #28) + Phase 9c Argus Investigator (PR #29). **Phase 9d Argus Magic Wands + Gemini provider pivot shipped 2026-05-10** (PR #30, commit `f2b59b3`) — provider abstraction in `lib/argus/llm/` (`types.ts`, `gemini-adapter.ts`, `index.ts`, JSONSchema → OpenAPI-3.0-subset translator); rip `@anthropic-ai/sdk` → swap to `@google/genai` 2.0.1; `MODEL_HAIKU/SONNET/OPUS` constants → `TIER_FAST/SMART` tier mapping (fast → `gemini-2.5-flash`, smart → `gemini-2.5-pro`); rewrite all three shipped route handlers (9a ping, 9b Copilot agentic loop, 9c Investigator forced-tool) on the abstraction; SSE wire formats preserved end-to-end; tool_choice → `toolConfig.functionCallingConfig = { mode: 'ANY', allowedFunctionNames }` (forced function call); existing 5 tool definitions audited + migrated `input_schema` → `parameters`; `ToolContext` gains `modelUsed` for accurate audit-row attribution; 5 new structured-output wand tools (`suggest_risk_matrix` / `suggest_finding_severity` / `suggest_verification_method` / `assess_reportability` / `draft_capa_metadata`) with prompt-cached system prompts; new non-streaming `/api/argus/wand` route handler with Zod-validated outputs + 24h cache on Reportability keyed by `incident.updated_at`; reusable `<ArgusMagicWand>` discriminated-union component + `<SuggestionCard>` (Accept/Edit/Reject) + `useArgusWand` fetch hook; mounts on Step 2 risk matrix · finding-actions card (informational) · CAPA verification-method dropdown · CAPA Create modal (when opened from investigation) · OSHA-301 + RIDDOR-F2508 single-incident pages (read-only auto-load, `print:hidden`); zero schema migration (reuses 9a tables — new `surface` strings on `argus_suggestions`); zero new RBAC keys (`argus:use` + the existing surface write permission); prompt caching dropped in v1 (Gemini cache uneconomical for our prompt sizes); default org daily token budget bumped 5M → 10M to reflect Flash pricing; env: `ANTHROPIC_API_KEY` → `GEMINI_API_KEY`. **Phase 9e Argus Global Panel + Insight Tiles shipped 2026-05-11** (PR #31, commit `b27c09d`) — closes Phase 9. New `lib/argus/page-context.ts` (route key + redacted record summaries + numeric aggregates + `hasActiveSignal`) + `<ArgusContextProvider>`/`<ArgusContextPayload>` race-safe last-mount-wins client provider mounted in `(app)/layout.tsx`; 8 page payloads (dashboard, /incidents/[id], /investigations/[id], /capa/[id], /inspections/[id], /capa, /inspections, /reports). Side panel reads context via `useArgusPageContext()` — header chip ("Incident IR-014 · UCB Houston"), per-route suggestion chips that auto-fill + auto-submit, posts new `panel_chat` surface to `/api/argus/stream` with the redacted page context (re-redacted server-side as defence-in-depth, system prompt in `lib/argus/system-prompts/panel.md`). Seven `<ArgusInsightTile>` mounts (4 dashboard: overdue investigations · stop-work active · reportability uncertain · CAPA overdue; 1 each on /capa /inspections /reports) share one route handler `app/api/argus/tile/route.ts` + one `tile_insight` structured-output tool — Zod-validated, smart-tier with `thinking: 'off'`. **Tiles never auto-fire** — each card opens in an idle state with an explicit "Analyse with Argus" button (user clicks to spend any tokens; the cyan dot on the Sparkles trigger is still set server-side from aggregator output so users see where attention is needed before clicking). `argus_suggestions` cache keyed on deterministic UUIDv5 of `tile + freshnessKey` means a second user on the same signal hits cache instantly. New `lib/argus/availability.ts` `isArgusAvailable(orgId)` server helper. New `tile` rate-limit bucket (20/min/user) auto-routed for `tile_*` surfaces. Topbar polish: Argus Sparkles button moves right of NotificationBell; cyan dot paints on the trigger when `pageContext.hasActiveSignal` is true (each tile-rendering page sets it from non-zero aggregates). Zero schema migration (8 new `surface` strings + `target_kind='page'` reuse the existing `text` columns). Zero new RBAC keys — tiles reuse existing `investigation:lead` / `capa:complete` / `inspection:read_site` / `report:read` / `incident:read_site`. **Phase 9 closed.** **Phase 7 Global Search shipped 2026-05-11** (PR #32, commit `f464d7e`) — demo-grade ⌘K command palette that hydrates the Phase 6l search shell. One route `app/api/search/route.ts` running `Promise.all` ILIKE fan-out across all 9 module entities (Incidents · Investigations · CAPAs · Inspections · Templates · Assets · Documents · Sites · People), `LIMIT 5` per group, RLS gates visibility per user, soft-delete (`deleted_at` / `archived_at`) filtered per table, empty groups dropped from the response. New `components/ui/command.tsx` shadcn Command primitive on top of `cmdk` 1.1.1 (one new dep, ~20 kB). New `lib/search/escape.ts` escapes `% _ , \\ )` so user input can't break PostgREST `.or()` filter syntax. `components/app-shell/search-trigger.tsx` palette body rewrite: 8 "Jump to" module shortcuts as the empty state (Dashboard + 7 module list pages); "Recent searches" group backed by `localStorage` key `argus.search.recent` (MRU 5, written only on successful navigation from a result); 200 ms debounced fetch with `AbortController`; `cmdk shouldFilter={false}` (server is the filter); global ⌘K / Ctrl+K binding with a guard so it doesn't hijack input/textarea/contentEditable elsewhere (still works from inside the palette's own input). No FTS / `tsvector` / GIN / `pg_trgm` — ILIKE is plenty for demo data; future migration to `.textSearch()` is a route-internal swap, API shape stays stable. Zero schema migration, zero new RBAC keys, no telemetry. **Phase 10 Safety Bulletins shipped 2026-05-11** (PR #33, commit `f8b503b`) — standalone post-investigation lessons-learned module per IMS_PLANNING §6.4.3 + §12.2.16. Demo-grade scope (author + list + view + dashboard widget); audience targeting + acknowledgement tracking + worker "unread" banner all deferred to Phase 10.5. **The "Report Wizard 3→4 step restructure" framing in the original Phase 10 description was dropped — bulletins describe lessons learned which workers don't know at report time; authoring belongs after investigation closure.** SPEC §15 gets a divergence note. Shipped: new `safety_bulletins` table + `safety_bulletin_status` enum + RLS (read = published OR archived OR author; insert = `bulletin:create`; update = author-of-draft OR `bulletin:publish`; no delete) + two permission keys (`bulletin:create` / `bulletin:publish` → `ehs_manager` + `site_admin`) + `seed_default_roles()` update so future orgs inherit. Routes: `/bulletins` (Published / My drafts / Archived filter chips), `/bulletins/new` (accepts `?from_investigation={id}` and pre-fills title + HTML body via `lib/bulletins/seed-from-investigation.ts`), `/bulletins/[id]` (sanitized HTML render via `isomorphic-dompurify`, Edit/Publish/Unpublish/Archive buttons gated on RBAC + ownership), `/bulletins/[id]/edit` (drafts only, author only). Server actions: `createBulletin` / `updateBulletin` / `publishBulletin` / `unpublishBulletin` / `archiveBulletin`. **Body is rich-text (TipTap, `@tiptap/react` + `@tiptap/starter-kit` + `@tiptap/extension-link`), NOT markdown** — EHS authors are not markdown-fluent; the editor exposes Bold/Italic/H2/H3/bullet/numbered list/blockquote/inline code/link/undo/redo and stores HTML in the existing `body text` column (no schema change). Mount points: sidebar entry between **Reports** and **Admin** (read implicit, no permission gate); dashboard widget `<LatestBulletinsCard>` (top-3 published org-wide, renders nothing if empty); investigation Summary tab `<BulletinCta>` gated on `closed && incident.track === 'A' && bulletin:create && no non-archived bulletin from this investigation` (swaps to "Bulletin drafted: …" link when one exists). New deps: `@tiptap/react` / `@tiptap/starter-kit` / `@tiptap/extension-link` / `isomorphic-dompurify` (replaced the short-lived `react-markdown` from the initial commit). **All v1-roadmap phases now shipped.** Next per `PLANNING/IMS_PLANNING.md` is the v2 surface — Document Control / Training / Audit / MOC / Permit / Inspection ITA + HSE submission / Risk Register / Contractor / Toolbox Talks / BBS / JSA / Emergency Management / Environmental — those are out of v1 scope and need a fresh scoping pass before any are picked up. **Topbar responsive fix shipped 2026-05-11 (PR #34, commit `994b2ee`)** — at 375px the topbar overflowed (7 controls in one row). Moved the SiteSwitcher into the mobile sidebar Sheet below `lg`; topbar at narrow widths is now just the icon cluster. Sheet variant uses an **inline collapsible list** (no Radix popover portal — the dropdown was breaking out of the Sheet edges). Same component (`components/app-shell/site-switcher.tsx`) with two internal variants: `TopbarSiteSwitcher` keeps the Radix dropdown for ≥ lg; `SheetSiteSwitcher` is a pure inline `<button>` + `<ul>` with `max-h-[40vh] overflow-y-auto`. Threaded site props through `SidebarShell` + `AppSidebar`; nav-item clicks now also `setOpenMobile(false)` so the Sheet auto-closes after navigation. Zero schema, zero new deps. Establishes a durable rule: **inside mobile Sheets / Drawers, prefer inline collapsibles over Radix Popover-portaled UI** — popovers portal to body and can overflow the drawer edge regardless of width caps. **§JSA Job Safety Analysis spec adopted 2026-05-11 (PR #36, commit `3517314`)** — second v2 module spec'd in `docs/SPEC.md` §JSA per OSHA 3071 / HSE INDG163. Spec only, no code. 5 new tables: `jsas` (5-state lifecycle `draft → under_review → approved → expired → archived`, `expires_at` default = `approved_at + 12 months`, `approved_by != created_by` enforced at DB CHECK), `jsa_steps` (`@dnd-kit`-reorderable, `UNIQUE (jsa_id, sequence)`), `jsa_step_hazards` (FKs into `hazard_candidates(id)` + `hazards(id)` — depends on §HZ shipping first), `jsa_step_controls` (same 5-level hierarchy as §HZ), `jsa_signoffs` (`UNIQUE (jsa_id, worker_id, signed_for_session)` so workers re-sign per shift). 4-step wizard (Identity → Steps → Hazards & Controls → Review & Approve). Selective step-hazard promotion to §HZ via `hazard_candidates` rows with `source_type='jsa'` — EHS picks per row, no auto-promotion. **Approver ≠ creator** invariant uses the project's three-layer pattern (DB CHECK + server action + UI), mirroring CAPA owner ≠ verifier. 5 proposed permission keys (`jsa:read_site / draft / approve / signoff / promote_step_hazard`). Shares the §HZ 5×5 risk matrix via `lib/risk/matrix.ts`. **Build order: §HZ → §JSA** (or combined into one v2 phase if scope allows). **§HZ Hazard Register spec adopted 2026-05-11 (PR #35, commit `1f33e2a`)** — new SPEC section locking the v2 Hazard Register module per ISO 45001 §6.1.2. Spec only, no code: 5 new tables (`hazards` 6-state lifecycle / `hazard_risk_assessments` history-preserving with 8 trigger types / `hazard_controls` 5-level hierarchy elimination→PPE / `hazard_candidates` review queue from 6 identification methods / `incident_hazard_links` causal·contributing·exposed_but_not_causal feedback loop), routes `/hazards/*` + `/hazards/candidates/*`, 8 components, 3 action files, 4 KPI tiles (identified · S4-S5 high-risk · overdue reviews · **PPE-only-control auditor warning**), 5 new permission keys. **Shared 5×5 risk matrix extracted to `lib/risk/matrix.ts`** so the existing incident severity engine + new hazard risk assessment consume one lookup; `computeResidual()` applies max-reduction by highest applied control level. Implementation queued for the v2 surface scoping pass — the spec lock-in means when v2 picks it up, the data shapes / route layout / matrix refactor are pre-agreed. SDS Manager integration stubbed in v1 (text column present, no live API). Every runtime change goes through a feature branch + PR per `.claude/rules/github-workflow.md`; direct push to `main` is reserved for doc-only updates the user explicitly asks for.
- **Database workflow:** `pnpm db:push` (apply migrations) and `pnpm db:types` (regen TS types) both load `SUPABASE_ACCESS_TOKEN` + `SUPABASE_DB_PASSWORD` from `.env.local` via `scripts/with-env-file.mjs` — no interactive `supabase login` needed. New migrations follow Supabase CLI's required `YYYYMMDDhhmmss_<name>.sql` timestamp prefix.
- **Industry types** seeded for v1: `healthcare`, `education`, `manufacturing`, `warehouse`, `office`, `construction`, `lab`. Drives template presets. Extensible by `site_admin`.
- **Pathways** offered at onboarding: `report_incidents`, `run_inspections`, `manage_assets`, `compliance_reports`, `manage_documents`. Personalize the dashboard; do NOT gate modules — every org gets every module.
- **Seed structure:** one org "UCB" with two top-level sites Houston (US) + Manchester (GB) plus 1–2 child sites under each to exercise hierarchy + include-children. All names are placeholders (see memory: `project_site_names.md`).
- **Type-specific incident data** uses sparse columns on `incidents` (NOT child tables) for v1; per-type child tables (per IMS_PLANNING §12.2) deferred to v2.
- **File storage** uses Supabase Storage with private buckets + path-prefix RLS.

## Path aliases

`@/components`, `@/lib`, `@/ui`, `@/hooks` (configured in `components.json` + `tsconfig.json`).

## Verification

Local boot:
```bash
pnpm install
pnpm dev
```
Supabase project: `https://uvziktjgrcosxirisiug.supabase.co` (URL + publishable key in `.env.local`; service role key needed for `scripts/seed.ts`).
